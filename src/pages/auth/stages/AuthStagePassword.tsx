import React from "react";

interface AuthStagePasswordProps {
    signup: boolean;
}

const AuthStagePassword: React.FunctionComponent<AuthStagePasswordProps> = (
    props
) => {
    return <></>;
};

export default AuthStagePassword;
