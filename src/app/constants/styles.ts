interface ConstantWidths {
    drawerWidth: 240;
}

export const drawerWidth = 240;

const pageStyle = {
    width: "100vw",
    height: "100vh",
};

export { pageStyle };

export default ConstantWidths;
